# V0.1.0 发布检查

日期：2026-09-25。来源为本地 V3 音乐播放器，目标仓库为 `RonaldDeng/Rhine-Music-Demo`，发布平台为 macOS。原工程与个人音乐数据未修改。

## 内容与清理

- 版本号及锁文件统一为 `0.1.0`，补充 Node.js `>=22.12.0` 要求。
- 保留全部运行源码、静态资源、内容数据、必要构建与服务脚本、模型源文件、生成脚本、上游作者署名和许可。
- 保留开发对照工具和原版说明引用的历史截图；`verification/README.md` 标明历史记录与本次发布验证的区别。
- 排除 `node_modules`、`dist`、系统杂项、交互会话记录、模型备份、缓存、日志和本地 V2 验证截图；删除旧模型审阅 PNG、参考裁图、Windows 启动器和上游网站部署配置。
- 个人音乐文件、曲库配置、索引和封面缓存不进入源码包；忽略规则覆盖 `music-data*/` 以及相关本地覆盖文件。
- README、设置提示和播放错误文案以 macOS 浏览器播放能力为准，不承诺 Windows 或 foobar2000 支持。
- 历史音乐验证文档移除实际曲库规模与试用专辑，文字审阅工具使用虚构样例。

## 构建与静态检查

在 macOS、Node.js 26.3.0、npm 11.16.0 环境中：

- `npm ci --no-audit --no-fund` 成功，使用已有锁定依赖，没有升级依赖版本。
- `npm run build` 成功：档案内容校验与导出、TypeScript 编译、Vite 生产构建和 PWA 清单生成全部完成。
- 既有共享渲染代码包仍有大于 500 kB 的 Vite 提示；这是体积警告，不是构建失败。本次没有为消除警告调整视觉或资源加载。
- `.command` 启动器通过 `bash -n`，全部 `.mjs` 脚本通过 `node --check`，Blender Python 脚本通过语法解析。
- 核对版本与锁文件一致性、当前说明的本地链接、待提交文件清单、常见凭据格式和本机路径；Git 差异无空白错误。

沿用用户自行体验和验收的约定，本次没有重新运行浏览器、真实设备、播放功能、全格式解码、线上资料接口或 Blender 图形界面验收。历史自动检查脚本保留，未在本次发布重新执行；Node.js 22.12 的最低声明未在独立运行环境复测。

## 模型目录元数据

`art/music-cd.blend` 的文件选择器目录字段 `FileSelectParams.dir[1282]` 已清空为根目录加零填充。严格解析并核对 1405 个 Blender block，解压前后数据长均为 609903 字节；只有一个目录字段改变，实际变更 39 字节，全部非目标字节完全一致。模型几何、材质、场景和 SDNA 未变。

文件以标准未压缩 Blender 格式保存，SHA-256 为：

```text
d291a0389db2037054f360e8506ddec3d2f142bb771769cae204d03c63a162e1
```

复用清理工具见 `scripts/sanitize_blend_browser_metadata.py`；该工具只接受已验证的 Blender 文件格式。没有用 Blender 实际打开验证，不能把字节及结构检查称为图形界面验收。

清理作用于本次发布的文件快照，保留已有 Git 历史，没有重写旧提交。
