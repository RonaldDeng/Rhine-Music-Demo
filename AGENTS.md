# Rhine Music Demo · V0.1.1

## 项目与发布

- 当前目录是基于 V0.1.0 的 V0.1.1 发布副本；原 V0.1.0、V3、V2、上游工程和个人音乐数据保留原状。
- 发布目标为 RonaldDeng/Rhine-Music-Demo 的 main 分支和 v0.1.1 标签。用户已授权本次整理、更新文档和推送；不能推送到 LBEILC/RhineLabUI。
- V0.1.1 仅支持 macOS。Windows 不在本次开发、验证或文档支持范围。
- 使用 TypeScript、Three.js、Vite 和 Node.js。音乐服务只监听 127.0.0.1；保留目录边界、Origin/Host 校验及只读音乐文件约束。
- 默认索引目录为工程旁的 music-data-v3，可用 MUSIC_DATA_DIR 覆盖。禁止提交真实歌曲、封面缓存、索引、配置、密钥和本机路径。

## 视觉与交互

- 沿用当前原生实现，不启用前端设计或动效 Skill。保留上游署名和第三方许可；不要改变已认可的视觉和动效以完成发布清理。
- 美术资源继续通过 Blender MCP 制作，保留源文件与可复现脚本；发布前清理源文件内与模型无关的个人路径元数据。
- 启动保留约 5.2 秒三维进场，不恢复 2D 片头、重播入口或自动打开专辑；跳过及减少动态效果直接进入待选界面。
- 音乐阵列每列使用 48 个循环位置；不增加真实专辑数量。盒体运行时为 4.45 × 3.35 × 0.14，封面 2.98 × 2.98，详情保留 8° 水平斜视。
- 抽取、转正、镜头左移、菜单进入及收回保持当前连续衔接。切换专辑等待阵列落稳；连续输入延续当前状态。
- 保留暖昼／深夜主题渐变、选中底板滑动、滑尺导航和歌名字轮。旧导航通过 ?nav=previous 保留对照。
- V0.1.1：竖屏下右侧深夜渐变遮罩以约 650ms 淡出，横屏恢复；保持上下边缘与文字阅读底色，减少动态效果直接切换。
- 根目录直属音频各为独立单曲专辑；子文件夹按含音频的文件夹归并。不改写音乐标签。

## 验证

- 沿用用户自行体验和验收约定：本次发布仅做源码静态检查、TypeScript 与生产构建，不追加浏览器检查或额外自动测试。
- 开发检查脚本保留供后续维护；历史 verification 记录不代表 V0.1.1 已重新执行同样的检查。
- 构建用 npm ci 和 npm run build。dist、node_modules、缓存、截图和备份不进入发布源码包。
- 原版入口 ?original=1 仍依赖 src、content 和 public 里的共享资源；不能仅按音乐界面删掉这些资源。
