# Rhine Music Demo · v0.1.0

用三维玻璃 CD 盒浏览和播放本地音乐。基于 [LBEILC/RhineLabUI](https://github.com/LBEILC/RhineLabUI) 改造，由 [RonaldDeng](https://github.com/RonaldDeng) 维护音乐适配。

**本版本整理自 V3，目前仅支持 macOS，暂不支持 Windows。** 播放器在本机运行，通过浏览器显示界面；不附带歌曲，也不是 `.app` 安装包。

[下载 v0.1.0](https://github.com/RonaldDeng/Rhine-Music-Demo/releases/tag/v0.1.0) · [更新记录](CHANGELOG.md) · [音乐服务说明](docs/MUSIC-SERVICE.md)

## 功能

- **本地曲库**：扫描指定音乐目录，读取歌曲标签和封面，增量更新索引，不修改音乐源文件。根目录直属音频显示为独立单曲卡片，子文件夹按专辑整理。
- **三维专辑架**：玻璃盒体、原比例封面、循环阵列，以及连贯的抽取、转正和收纳动作。加载后播放约 5.2 秒的三维进场，可跳过；进入后由你选择专辑。
- **查找与排列**：支持搜索，按流派、歌手或专辑名排列；底部滑尺随选择移动，跨类别及首尾连续切换。保留 `?nav=previous` 旧导航回退入口。
- **播放与详情**：点击歌单中的歌曲开始播放，连续播放当前专辑；提供播放、暂停和停止，展示格式、时长等元数据及有来源的专辑介绍。
- **外观与声音**：暖昼／深夜主题平滑切换，歌名滚动过渡，横屏与竖屏布局；歌曲、氛围配乐和界面音效独立调节，提供画质设置与减少动态效果。

## 快速开始（macOS）

需要 [Node.js](https://nodejs.org/) **22.12 或更新的 LTS 版本**（包含 npm），以及支持 WebGL 2 的现代浏览器。普通运行无需 Blender。

1. 从 [Release](https://github.com/RonaldDeng/Rhine-Music-Demo/releases/tag/v0.1.0) 下载 `Rhine-Music-Demo-v0.1.0-macOS.zip`，完整解压到本地文件夹。
2. 双击文件夹中的 **`启动音乐播放器.command`**。首次启动会联网执行 `npm ci`、构建界面，并自动打开默认浏览器；后续按需重建。
3. 在播放器的“音乐库”中填写音乐文件夹的**绝对路径**，例如 `/Users/你的用户名/Music`，保存并扫描。扫描成功后界面会自动刷新。

若双击无法运行，可在终端进入解压后的文件夹并执行：

```sh
bash "启动音乐播放器.command"
```

若只是可执行权限丢失，可运行 `chmod +x "启动音乐播放器.command"` 后重新双击。首次准备需要网络；依赖安装和构建完成后，本地浏览与播放不需要在线资料服务。

启动器优先使用 `5175` 端口，端口被占用时选择后续空闲端口；已运行的同一工程服务会直接复用。服务就绪后可关闭启动器终端，后台服务会继续运行。日志位于工程旁的 `music-data-v3/player-service.log`。

### 命令行运行

```sh
git clone https://github.com/RonaldDeng/Rhine-Music-Demo.git
cd Rhine-Music-Demo
npm ci
npm run build
npm run music -- --port 5175
```

打开 [本地播放器](http://127.0.0.1:5175/)，以终端输出的地址为准。命令行服务保持终端运行，按 `Ctrl+C` 停止。

音乐服务只监听本机 `127.0.0.1`，同时提供界面和音乐 API。`npm run dev`、`npm run preview` 单独运行仅提供前端，不能替代音乐服务。修改前端后重新构建并刷新；修改服务脚本后重启服务。

## 常用操作

| 操作 | 功能 |
| --- | --- |
| `← / →` | 切换当前排列方式的分类，首尾循环 |
| `↑ / ↓` | 切换当前分类中的专辑，首尾循环 |
| `Enter` | 打开当前专辑；进场期间跳过进场 |
| 点击歌单中的歌曲 | 从所选曲目开始播放 |
| `Space` | 播放／暂停；尚未选曲时播放当前专辑 |
| 右上播放／暂停、停止 | 控制播放；停止后恢复已启用的氛围配乐 |
| `/` 或“搜索” | 搜索曲库 |
| `Esc` | 关闭弹窗、返回专辑架；进场期间跳过进场 |
| 在详情卡片上拖动 | 旋转查看玻璃 CD 盒 |

输入框内不会触发曲库导航快捷键。设置中可调整排列方式、主题、声音、画质和在线资料。

## 数据与升级

默认数据目录为工程旁的 **`../music-data-v3/`**，存放曲库路径配置、索引、封面缓存、流派规则与服务日志；歌曲仍从原路径读取。这些个人数据不包含在 GitHub 仓库或发布 ZIP 中。

从本地 V3 升级时，先停止旧版音乐服务，把 `V0.1.0` 放在与原 V3 工程相同的父目录，即可继续使用旁边的 `music-data-v3`。不要同时运行两个服务写入同一个数据目录。移动工程时，也要保留原数据目录位置，或显式指定 `MUSIC_DATA_DIR`。

较早版本使用的 `../music-data/` 可在停止旧服务后复用。在新版工程目录运行：

```sh
MUSIC_DATA_DIR="../music-data" bash "启动音乐播放器.command"
```

根目录直属音频会在重扫后由旧的合辑改为独立单曲；旧合辑的人工流派分类不会自动分配给新单曲。子文件夹仍按每个含音频的文件夹识别专辑，不同 CD 子文件夹分别识别。移动音频或专辑文件夹会产生新 ID；暂时断开的音乐根目录保留已有缓存。

## 在线资料与支持范围

“查询／更新专辑介绍”从公开百科查询文字，核对专辑与歌手后保存介绍及来源。流派和制作人员可选用 MusicBrainz 补全，需填写自己的联系邮箱或项目网址作为请求标识；自动补全默认关闭。查询只发送所需名称或公开实体 ID，不上传音乐文件。未找到、匹配不明确和网络失败会显示各自状态，真实资料覆盖率仍受来源及网络条件影响。

可以索引 FLAC、WAV、M4A、MP3、DSF、DFF 等音频的元数据，**实际播放取决于浏览器及具体编码**；能读取标签不等于能播放，也不代表已验证无损或指定音频设备输出。DSF／DFF 暂不能在当前浏览器播放器中播放。

当前不包含歌词、频谱、均衡器、在线搜歌或云同步。音乐模式未开放独立 360° 查看器、拆解和重组入口。高画质会增加渲染开销，帧率偏低时可在设置中降低画质或关闭景深，实际效果取决于设备和窗口尺寸。

本次发布的验证范围为生产构建与静态检查，结果见 [发布检查](docs/RELEASE-V0.1.0.md)；未重新进行浏览器、真实设备、全格式播放或在线资料覆盖率验收。

## 开发与来源

界面使用 TypeScript、Three.js 和 Vite；本地服务使用 Node.js 与 `music-metadata`。保留运行源码、锁定依赖清单、必要资源与生成脚本。发布 ZIP 不包含 `node_modules`、`dist` 或个人曲库数据，运行时按需安装和构建。

初始基于上游提交 [`5abab02367465d9189f4ae65bcb6f17fdb5938f7`](https://github.com/LBEILC/RhineLabUI/commit/5abab02367465d9189f4ae65bcb6f17fdb5938f7)。保留上游作者署名、提交历史、[LICENSE](LICENSE) 和 [README.original.md](README.original.md)。[上游在线展示](https://rhine.lubeiluchen.cc/) 是原版档案终端，音乐播放器以本仓库的本地版本为准。

原工程有权授权的程序代码、建模脚本及技术文档沿用 MIT 许可，并保留 **Copyright (c) 2026 LBEILC**。音乐适配不表示上游作者参与或认可本衍生版本；代码许可也不自动覆盖全部模型、图片、字体、音频及原作视觉内容。

- 参考作品：《明日方舟》特别映像[「莱茵生命：访问」](https://www.bilibili.com/video/BV1rr4y1b7sz/)。相关名称、标志、设定及原作内容的权利归各自权利人所有，本项目与官方制作方无隶属关系。
- MiSans：[版权说明](public/fonts/NOTICE.txt)、[字体许可](public/fonts/MiSans-license.pdf)。
- Rolling Number：[Kit Langton 的 MIT 许可](public/licenses/rolling-number.txt)。
- 音频：[配乐与原 PV 短音的来源和许可边界](public/audio/README.md)。
- 演示封面：[生成来源说明](public/demo-covers/README.md)。真实歌曲、封面和个人缓存不随项目分发；在线介绍保留来源及相应许可。

完整资源说明见 [README.original.md](README.original.md#参考与资源说明)，其他依赖遵循各自许可。
